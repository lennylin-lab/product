# 执行计划

> 严禁 Bash cd（hook 相对路径），绝对路径操作。

1. SchedulingSnapshotLoader：助手 + 四方法分块 + 文案 + 删 requireBoundedIds + javadoc 口径。
2. SchedulingSnapshotLoaderTest：+3 用例。
3. 三个 init 脚本 TRUNCATE 段 + 运维手册标注。
4. `mvn -f /home/lenny/Projects/pps/product/product-services/pom.xml -B -ntp -pl product-planning -am test`；grep requireBoundedIds 归零。
5. trellis-check → 提交（#13/#14 各一 commit）→ 推送 → 评论关闭 → 归档记日志。
