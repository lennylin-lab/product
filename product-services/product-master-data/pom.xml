<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.product</groupId>
        <artifactId>product-services</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>product-master-data</artifactId>

    <name>product-master-data</name>
    <description>product-master-data: 主数据域服务骨架（产品、工艺路线、工序规则、资源、机台、模具、能力、日历、换型规则；ADR-0002）</description>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-tracing-bridge-otel</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        </dependency>
        <dependency>
            <groupId>com.product</groupId>
            <artifactId>product-cloud-common</artifactId>
        </dependency>
        <!-- 认证基线：本地验签安全链（ADR-0003：服务端两层校验的服务侧，
             直连服务端口必须携带有效签名 JWT；servlet 安全链由 cloud-security 自动装配） -->
        <dependency>
            <groupId>com.product</groupId>
            <artifactId>product-cloud-security</artifactId>
        </dependency>
        <!-- 主数据服务契约：实现 MasterDataBatchQueryApi（/internal/master-data 批量查询端点） -->
        <dependency>
            <groupId>com.product</groupId>
            <artifactId>product-master-data-api</artifactId>
        </dependency>
        <!-- Phase 4：启用工艺路线删除保护需要跨域确认——demand 契约（按产品取订单行）
             + planning 契约（订单行下是否存在阻塞工序任务）。只读查询经 OpenFeign，
             禁止跨服务读写他方表（ADR-0005 §2） -->
        <dependency>
            <groupId>com.product</groupId>
            <artifactId>product-demand-api</artifactId>
        </dependency>
        <dependency>
            <groupId>com.product</groupId>
            <artifactId>product-planning-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-openfeign</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-loadbalancer</artifactId>
        </dependency>

        <!-- 数据访问：MyBatis-Plus（版本由根 POM 管理，与单体一致）；master_data_db 独立数据源（ADR-0005） -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-jsqlparser</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
        </dependency>

        <!-- Excel 导入导出（产品/机台/日历 /export /importTemplate /importData；版本由根 POM 管理） -->
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi-ooxml</artifactId>
        </dependency>

        <!-- Lombok（实体 @Data/@EqualsAndHashCode，与单体 domain 实体同款） -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
